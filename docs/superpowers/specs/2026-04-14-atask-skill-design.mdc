# atask 任务系统 Skill 设计稿

## 1. 背景与目标

当前在使用 Claude Code、Codex 等终端型 AI agent 时，即使任务开始前已经写出了较详细的计划和清单，agent 在实施过程中仍然可能漏掉计划中的某些关键步骤。问题的根源不在于“有没有计划”，而在于缺少一套可持续、可校验、可持久化的执行约束载体。

本设计的目标是定义一套面向 AI 使用的轻量任务系统，并配套一个必须使用的 skill。该系统要求 AI 在计划阶段先定义稳定的任务标识，在实施阶段通过统一的 CLI 显式维护任务状态，从而把“计划”转化为“可执行且可检查的任务面”。

第一阶段不追求复杂功能，不做多文件拆分，不做并发占用，不做自动计划解析，不做 GUI 编辑。第一阶段的重点是建立一个简单但足够硬的执行闭环，解决“任务会漏”和“状态会补账”这两个核心问题。

## 2. 设计原则

### 2.1 轻量化优先

一个工程仅维护一个任务文件，固定路径为 `/.project.atask`。任务数据不再拆分成额外伴生文件，减少系统噪音和维护负担。

### 2.2 计划与执行分层

计划文档是规划真源，负责定义主任务和子任务的稳定标识与语义边界。`/.project.atask` 是执行真源，负责维护实际任务树和执行状态。

### 2.3 强制 CLI，弱化手工改写

日常任务写操作必须通过 `atask` CLI 完成，不允许 AI 在常规流程中直接改写 `/.project.atask`。只有在 CLI 本身故障、需要修复真源时，才允许极少数维护性直接编辑。

### 2.4 标识先于执行

主任务 `key` 与子任务 `id` 都属于计划层约束，必须在计划文档中先定义好，再进入实施。CLI 的职责是落库、校验、状态流转，而不是替计划临时生成关键标识。

### 2.5 为 GUI 预留但不为 GUI 过度设计

底层真源采用 JSON，文件后缀采用 `.atask` 专用语义，但文件内容使用结构化 JSON，以便后续 CLI 校验与 GUI 渲染直接复用同一份数据模型。

## 3. 核心真源模型

工程根目录固定存在单文件：

`/.project.atask`

该文件内容为 JSON，对应一个项目级任务对象。第一版最小结构如下：

```json
{
  "ver": 1,
  "title": "ctask",
  "tasks": [
    {
      "key": "cli-core",
      "title": "CLI 核心能力",
      "desc": "实现 atask 的基础命令与任务写入规则",
      "st": "todo",
      "ct": "2026-04-14T16:00:00+08:00",
      "dt": null,
      "tasks": [
        {
          "id": "t1",
          "title": "实现 init 命令",
          "desc": "创建 /.project.atask",
          "st": "todo",
          "ct": "2026-04-14T16:05:00+08:00",
          "dt": null,
          "tasks": []
        },
        {
          "id": "t2",
          "title": "输出任务树视图",
          "desc": "展示任务层级和状态",
          "st": "doing",
          "ct": "2026-04-14T16:06:00+08:00",
          "dt": null,
          "tasks": [
            {
              "id": "t2.1",
              "title": "渲染缩进树结构",
              "desc": "支持父子任务层级展示",
              "st": "todo",
              "ct": "2026-04-14T16:08:00+08:00",
              "dt": null,
              "tasks": []
            }
          ]
        }
      ]
    }
  ]
}
```

## 4. 字段定义

项目级字段：

- `ver`: schema 版本号
- `title`: 工程或任务域标题
- `tasks`: 主任务列表

主任务字段：

- `key`: 主任务稳定锚点，来自计划文档
- `title`: 主任务显示标题
- `desc`: 主任务简短说明
- `st`: 主任务状态
- `ct`: 创建时间
- `dt`: 完成时间，未完成时为 `null`
- `tasks`: 子任务列表

子任务字段：

- `id`: 子任务稳定标识，来自计划文档
- `title`: 子任务标题
- `desc`: 子任务简短说明
- `st`: 子任务状态
- `ct`: 创建时间
- `dt`: 完成时间，未完成时为 `null`
- `tasks`: 子任务列表

### 4.1 状态枚举

第一版限定为：

- `todo`
- `doing`
- `blocked`
- `done`
- `cancelled`

### 4.2 时间字段

第一版仅保留最小时间集：

- `ct`: 创建时间
- `dt`: 完成时间

不引入 `updated_at`、`due_at`、`archived_at` 等扩展字段，以降低 agent 的写入负担。

## 5. 计划文档约束

为了确保“计划”和“执行”之间存在稳定映射，skill 必须在计划阶段强制以下规则。

### 5.1 主任务必须定义 key

计划文档中的每个主任务都必须显式定义一个稳定 `key`。该 `key` 一旦进入计划文档，即作为执行阶段的主任务锚点，不应在实施过程中随意更改。

推荐写法：

```mdc
## [task:cli-core] CLI 核心能力
实现 atask 的基础命令与任务写入规则。
```

### 5.2 子任务必须定义稳定 id

计划文档中的关键子任务也必须显式定义稳定 `id`。该 `id` 不由 CLI 自动生成，而由计划者在计划阶段写明，后续执行阶段沿用。

推荐写法：

```mdc
## [task:cli-core] CLI 核心能力

- [t1] 实现 init 命令
- [t2] 输出任务树视图
- [t3] 新增主任务与子任务
- [t4] 校验状态流转规则
```

多层级示例：

```mdc
## [task:cli-core] CLI 核心能力

- [t1] 实现 add 命令
  - [t1.1] 支持新增主任务
  - [t1.2] 支持新增子任务
  - [t1.3] 校验父任务状态限制
- [t2] 输出任务树视图
```

### 5.3 子任务命名规范

计划文档中的子任务标题必须符合统一命名规范：

- 使用单一可执行动作
- 使用“动词 + 对象 + 必要限定”结构
- 一个子任务只表达一个可检查结果
- 避免模糊、泛化、混合多个动作的标题

推荐示例：

- `实现 init 命令`
- `输出任务树视图`
- `校验父任务完成约束`
- `拒绝删除两周内已完成任务`

不推荐示例：

- `完善 CLI`
- `处理任务逻辑`
- `把各种边界情况都处理一下`
- `实现并测试并优化 tree 命令`

### 5.4 计划阶段失败条件

出现以下任一情况，skill 不允许进入实施阶段：

- 缺失主任务 `key`
- 缺失关键子任务 `id`
- 子任务标题不符合最小命名规范

## 6. 状态流转规则

第一版仅允许最小必要流转：

- `todo -> doing`
- `doing -> done`
- `doing -> blocked`
- `doing -> cancelled`
- `blocked -> doing`
- `todo -> cancelled`

默认不允许：

- `todo -> done`
- `done -> doing`
- `cancelled -> doing`
- 任意跨级跳转

状态流转的目标是强制 AI 显式经历“开始执行”的动作，而不是通过收尾补账伪造执行过程。

## 7. 父子任务约束

### 7.1 父任务完成约束

父任务只有在全部直接或间接子任务均处于以下状态时，才允许标记为 `done`：

- `done`
- `cancelled`

若存在任一未结束子任务，CLI 必须拒绝父任务完成操作。

### 7.2 父任务新增子任务限制

当主任务或父子任务节点处于以下状态时，允许新增子任务：

- `todo`
- `doing`
- `blocked`

当其状态为以下值时，不允许继续新增子任务：

- `done`
- `cancelled`

### 7.3 状态不自动联动

第一版不做复杂自动联动：

- 子任务开始执行时，不自动修改父任务状态
- 新增子任务时，不自动重写父任务状态
- 父任务状态由显式命令控制

这样可以减少隐藏逻辑，保持 CLI 行为可预测。

## 8. 删除与清理规则

第一版仅允许显式清理，不做自动删除。

删除条件必须同时满足：

- `st = done`
- `dt` 距当前时间超过 14 天

以下情况一律不允许删除：

- 非 `done` 状态任务
- `done` 但完成时间未超过 14 天的任务

清理由 `atask prune` 显式触发，默认建议先 dry-run 展示。

## 9. CLI V1 设计

### 9.1 设计原则

- 所有常规写操作必须通过 CLI
- CLI 不负责自动解析计划全文
- CLI 不负责替计划层生成关键标识
- CLI 负责创建、更新、校验、可视化

### 9.2 命令集

#### `atask init`

作用：

- 创建根目录 `/.project.atask`
- 若文件已存在则拒绝覆盖
- 项目标题可传参，不传时可取目录名

#### `atask tree`

作用：

- 输出树形任务视图
- 最少展示 `key/id`、`st`、`title`
- 可选展示 `desc`、`ct`、`dt`

该命令是 V1 的核心人机检查入口。

#### `atask add`

作用：

- 新增主任务
- 新增子任务
- 新增多层级子任务

主任务新增示例：

```bash
atask add --task-key cli-core --title "CLI 核心能力" --desc "实现基础命令"
```

子任务新增示例：

```bash
atask add --task cli-core --id t1 --title "实现 init 命令" --desc "创建 /.project.atask"
atask add --task cli-core --id t2 --title "输出任务树视图" --desc "展示任务层级和状态"
atask add --task cli-core --parent t2 --id t2.1 --title "渲染缩进树结构" --desc "支持父子任务层级展示"
```

规则：

- 新增主任务时必须传 `--task-key`
- 新增子任务时必须传 `--task <key>` 和 `--id <id>`
- 若新增某个父任务下的子任务，必须显式传 `--parent`
- CLI 必须校验同层与同主任务作用域内的标识唯一性

#### `atask start`

作用：

- 将指定任务切换为 `doing`

示例：

```bash
atask start --task cli-core --id t2
```

CLI 需校验：

- 主任务存在
- 子任务存在
- 状态允许切换到 `doing`

#### `atask done`

作用：

- 将指定任务切换为 `done`
- 写入 `dt`

CLI 需校验：

- 状态允许从当前态进入 `done`
- 若存在未结束子任务，则拒绝完成

#### `atask block`

作用：

- 将指定任务切换为 `blocked`

#### `atask cancel`

作用：

- 将指定任务切换为 `cancelled`

#### `atask validate`

作用：

- 校验 schema
- 校验主任务 `key` 唯一性
- 校验子任务 `id` 唯一性
- 校验状态值合法性
- 校验状态流转约束
- 校验父任务完成约束
- 校验删除规则前置条件

#### `atask prune`

作用：

- 显式清理满足删除条件的历史已完成任务

建议：

- 默认 dry-run
- 显式确认后执行真正删除

## 10. Skill V1 强制工作流

### 10.1 计划阶段

skill 必须要求：

- 主任务定义 `key`
- 子任务定义稳定 `id`
- 子任务标题符合命名规范

否则不允许进入实施。

### 10.2 执行准备阶段

实施前必须完成：

1. 检查 `/.project.atask` 是否存在
2. 若不存在，则执行 `atask init`
3. 通过 `atask add` 显式创建计划中的主任务和关键子任务

此阶段不允许直接手改 JSON。

### 10.3 实施阶段

开始具体工作前必须：

```bash
atask start --task <key> --id <id>
```

完成或中断后必须立即执行：

- `atask done`
- `atask block`
- `atask cancel`

若没有处于 `doing` 的当前任务，则 AI 不应进入实施动作，也不应声称该任务已在执行。

### 10.4 自检与收尾

在关键节点必须使用：

- `atask tree`
- `atask validate`

只有在需要清理历史完成项时，才使用：

- `atask prune`

## 11. 非目标

第一版明确不做以下内容：

- 多文件任务拆分
- owner / 并发锁机制
- 自动解析计划文档并同步任务
- 自动编号生成关键子任务标识
- 复杂查询 DSL
- 任务依赖图
- GUI 编辑
- 评论流、日志流、审计流
- 自动清理后台任务

## 12. 渐进式功能规划

### 12.1 V1：核心闭环

目标：

- 建立计划标识、任务真源、CLI 写操作、状态流转、自检校验的最小闭环

范围：

- `/.project.atask` schema
- `atask init`
- `atask add`
- `atask start`
- `atask done`
- `atask block`
- `atask cancel`
- `atask tree`
- `atask validate`
- `atask prune`
- Skill 的实施前置约束与执行中约束

成功标准：

- AI 不再轻易漏掉计划中的关键步骤
- AI 能稳定地将当前工作绑定到具体子任务
- 人能通过终端树视图快速检查当前进展

### 12.2 V1.5：约束增强与体验优化

目标：

- 在不改变真源模型的前提下，强化规则提示与可读性

范围：

- 更严格的命名规范检查
- 更友好的错误提示
- 更好的终端树视图过滤与排序
- 更显式的规则失败原因输出

成功标准：

- CLI 提示足够清晰，AI 更容易修正错误
- 人查看当前任务面时无需直接阅读 JSON

### 12.3 V2：GUI 看板

目标：

- 基于 `/.project.atask` 构建统一可视化看板

范围：

- 只读或有限编辑的 GUI 看板
- 按主任务分组展示
- 展示 `todo / doing / blocked / done / cancelled`
- 支持父子层级折叠
- GUI 与 CLI 共享同一真源和校验逻辑

成功标准：

- GUI 不引入第二真源
- 人可以快速看见当前 doing、blocked 与总体完成面
- 不因引入 GUI 而破坏 V1 CLI 的稳定性

## 13. 总结

这套设计的核心不是“再做一个待办清单”，而是把 AI 从“看计划自由发挥”转向“围绕稳定任务标识和显式状态流转实施工作”。

关键约束是：

- 一个工程一个 `/.project.atask`
- 底层 JSON 真源
- 主任务 `key` 与子任务 `id` 由计划阶段先定义
- 日常写操作一律通过 `atask` CLI
- 开始实施前必须把目标任务切换到 `doing`
- 完成后必须立即回写状态

在此基础上，V1 先解决执行纪律与可见性，V1.5 优化使用体验，V2 再自然过渡到 GUI 看板。这条路径能以最小复杂度，尽快解决当前 AI 漏项和执行不规范的问题。
